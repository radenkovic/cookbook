properties:
  version:
    type: string
  name: 
    type: string
  
  description:
    type: string
  
  files:
    type: array
    items:
      required:
        - path
        - strategy
      properties:
        path:
          type: string
        strategy:
          type: string
          enum: 
            - replace
            - skip-if-exists
            - merge-left
            - merge-right
        destination:
          type: string
  
  tags:
    type: array
    items:
      required:
        - name
        - description
      properties:
        name:
          type: string
        description: 
          type: string
      
    
required: 
  - version
  - name
  - files
    
