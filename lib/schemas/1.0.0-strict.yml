properties:
  version:
    type: string
  name: 
    type: string
  
  description:
    type: string
  
  files:
    type: array
    items:
      required:
        - path
        - strategy
      additionalProperties: false
      properties:
        path:
          type: string
        strategy:
          type: string
          enum: 
            - replace
            - skip
            - skip-if-exists
            - merge
            - merge-left
            - merge-right
            - remove
            - remove-if-exists
        destination:
          type: string
  
  tags:
    type: array
    items:
      required:
        - name
        - description
      properties:
        name:
          type: string
        description: 
          type: string
    additionalProperties: false
      
    
required: 
  - version
  - name
  - files
  
additionalProperties: false
    
